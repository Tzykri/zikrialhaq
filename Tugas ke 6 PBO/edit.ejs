<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Produk</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Edit Produk</h1>
        <form action="/update/<%= product.id %>" method="POST" onsubmit="return confirm('Apakah Anda yakin ingin memperbarui data produk ini?');">
            <div class="form-group">
                <label for="nama_produk">Nama Produk:</label>
                <input type="text" id="nama_produk" name="nama_produk" value="<%= product.nama_produk %>" required>
            </div>
            <div class="form-group">
                <label for="harga">Harga:</label>
                <input type="text" id="harga" name="harga" value="<%= product.harga %>" required>
            </div>
            <button type="submit">Update</button>
        </form>
        <a class="back-link" href="/">Kembali ke Daftar</a>
    </div>
</body>
</html>
